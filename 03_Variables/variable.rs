fn main() {
    println!("Hello, world!");
    let two = 2; // variable store integer value
    let hello = "hello"; // variable store string value
    let j = 'j';
    let my_half = 0.5;
    let mut my_name = "Roman"; // this is a mutable variable so, it means that we can change the value of this variable later
    let quit_program = false;
    let your_half = my_half; // assigning different variable value 0.5
}

